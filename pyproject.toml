[project]
name = "dependabot-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3==1.0.0",
    "scipy<1.16",
]

[dependency-groups]
dev = [
    "pytest==7.0.0",
    "pytest-cov==4.0.0",
]
